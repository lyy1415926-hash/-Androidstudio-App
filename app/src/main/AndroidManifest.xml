<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.campusinfoapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="校园信息助手"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusInfoApp">

        <!-- 主界面 -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 登录模块 -->
        <activity android:name=".ui.login.LoginActivity" android:exported="false" />

        <!-- 注册模块 -->
        <activity android:name=".ui.login.RegisterActivity" android:exported="false" />

        <!-- 新闻模块 -->
        <activity android:name=".ui.news.NewsActivity" android:exported="false" />
        <activity android:name=".ui.news.NewsDetailActivity" android:exported="false" />

        <!-- 课程表模块 -->
        <activity android:name=".ui.schedule.ScheduleActivity" android:exported="false" />

        <!-- 天气模块 -->
        <activity android:name=".ui.weather.WeatherActivity" android:exported="false" />

    </application>

</manifest>
